<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted only for the emulator's view of the developer's own
  machine (10.0.2.2 is the host loopback), which is where `wrangler pages dev`
  serves SitRep during local development. Every other destination, including
  the deployed Cloudflare Pages site, must be HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
